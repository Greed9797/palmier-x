name: Build Windows

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable

      - run: flutter config --enable-windows-desktop
      - run: flutter pub get

      - name: Analyze
        run: flutter analyze lib

      - name: Build Windows release
        run: flutter build windows --release

      - name: Bundle ffmpeg
        shell: pwsh
        run: |
          $url = 'https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip'
          Invoke-WebRequest -Uri $url -OutFile ffmpeg.zip
          Expand-Archive ffmpeg.zip -DestinationPath ffmpeg_extract
          $exe = Get-ChildItem -Recurse ffmpeg_extract -Filter ffmpeg.exe | Select-Object -First 1
          $dest = 'build\windows\x64\runner\Release\ffmpeg'
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          Copy-Item $exe.FullName "$dest\ffmpeg.exe"

      - name: Zip release
        shell: pwsh
        run: Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath PalmierX-windows.zip

      - uses: actions/upload-artifact@v4
        with:
          name: PalmierX-windows
          path: PalmierX-windows.zip
